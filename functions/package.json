{
  "name": "motionlab-functions",
  "description": "Cloud Functions for MotionLab — a server-side OpenAI proxy that keeps the API key off the client.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.1",
    "mathjs": "^15.2.0",
    "openai": "^4.67.3"
  }
}
