## Project Overview

**MotionLab** is a Brilliant-inspired interactive web app for teaching AP Physics C kinematics to high school students. The core philosophy: learn by doing — students are dropped into problems, graphs, and simulations before any explanation. The 7 lessons (Learn/Practice/Quiz) are hand-authored; the **AI Practice Hub** (`/practice`) is the one AI-powered surface, generating adaptive problems via a server-side OpenAI Cloud Function (`gpt-4o-mini`) behind a global toggle that is OFF by default — with AI off it runs on the static practiceBank.

- **Tech Stack:** React 18 + Vite 8 + TypeScript + Tailwind CSS v3
- **Backend:** Firebase Auth (email/password) + Cloud Firestore
- **Dev Environment:** Firebase Emulator Suite with `demo-motionlab` project ID; portable Temurin JDK 21 (arm64) stored in `.tools/` (gitignored), JAVA_HOME wired into npm scripts
- **Testing:** Vitest (unit + integration), Playwright (e2e). All 7 lessons have Learn + Practice + Quiz.
- **Routing:** React Router v6; `react-router-dom`
- **Firebase project:** `motionlab-3e44a` — **deployed live at https://motionlab-3e44a.web.app** (`npm run deploy`)
- **AI:** Firebase AI Logic (Gemini Developer API, free tier) via `firebase/ai`. Requires one-time provisioning: `npx -y firebase-tools@latest init ailogic` (enables the Gemini Developer API). App Check is NOT yet configured — REQUIRED follow-up before heavy production use (protects the AI quota).
- **Fonts:** Space Grotesk (display) + Inter (body), loaded via Google Fonts
- **Skills:** `frontend-design` symlinked to `~/.claude/skills/frontend-design` → auto-loaded by Cursor

---

## ⏳ Pending Setup (deferred — do later)

Code is complete and all tests pass; these are operator/account steps, NOT code changes.

1. **Re-authenticate the Firebase CLI** (the local token is expired → both `npm run deploy` AND `npm run deploy:rules` fail with "Your credentials are no longer valid"):
   ```bash
   firebase login --reauth        # interactive browser; use the account that owns motionlab-3e44a
   npm run deploy                 # redeploys hosting + firestore:rules + firestore:indexes
   ```
   - No API key needed for deploying — just reauth. The `Assertion failed: resolving hosting target...` error was only a side effect of being logged out.
   - **⚠️ The cross-user Leaderboard is non-functional on the LIVE site until the new `leaderboards/**` rules are deployed** (reads/writes are denied). `npm run deploy` (or `npm run deploy:rules`) pushes them. Until then it only works against the local emulator.

   Verified after the responsive-layout `/frontend-design` pass landed: `npm run build` (tsc + vite) passes clean and `npx tsc --noEmit` is clean. Still worth a quick in-app visual check of the widened layout + bigger basketball court + leaderboard opt-in before deploying.

   **Dead-code:** the codebase was swept with `knip`/`ts-prune` (the `/dead-code` skill's `tldr` binary isn't installed). It's essentially clean — only 2 truly-dead symbols were removed (`finishQuiz` in `e2e/helpers.ts`, an unused `export { todayISO, computeStreak }` re-export in `progressService.ts`). DO NOT chase the rest of knip's "unused exports" list: items like milestone tier constants, `MARGIN`/`AssistGuide`/`CUE_COLOR`/`otherGroup`/`otherSide`/`cueStart` (pool), the hook `*Phase` types, `content.ts` types, `launchVelocity`, `solveAiShot`, etc. are all USED INTERNALLY in their own module (knip only flags the redundant `export` keyword) — removing them breaks things; de-exporting is churn. Also IGNORE knip's "unused files" (`functions/index.js` is the Cloud Function entry; `playwright.preview.config.ts`/`vitest.integration.config.ts` are used by the `smoke`/`test:integration` npm scripts).

2. **AI questions are now an OpenAI Cloud Function** (`functions/generatePracticeQuestion`), gated behind a GLOBAL toggle that is **OFF by default** (`src/lib/aiSettings.ts`). With AI off, the whole app runs on the static practiceBank with no network calls (the MVP). To enable AI live: deploy the function + set the `OPENAI_API_KEY` secret, then flip the in-app AI toggle (header / Profile → Settings).
   - The function verifies math answers with `mathjs` (regenerates on contradiction) so a wrong math answer never ships; run `npm install` in `functions/` and redeploy the function after dependency changes.
   - Until the function is deployed (or with AI off), AI-backed surfaces gracefully use the bank. Everything else (lessons, quizzes, games, badges, nav) works without it.
   - The AI function requires the **live** Firebase config + deployed secret (won't run against the demo emulator without a local secret).

3. **(Recommended) Enable Firebase App Check** before relying on AI in production — protects the Gemini quota from abuse (reCAPTCHA Enterprise setup; not yet wired).

---

## Target Persona

**Bob** — 11th grade AP Physics C student struggling with kinematics. Needs short, visual, interactive lessons with instant feedback and progress tracking.

---

## Course Structure: AP Physics C Kinematics (7 lessons)

> App-facing course title is **"AP Physics C Kinematics"** (the "Motion Through Calculus" suffix was removed at the user's request).

### Lesson 1: Position, Velocity, and Slope
- **Core idea:** Velocity = derivative of position; v = dx/dt
- **Covers:** Position-time graphs, slope as average velocity, tangent slope as instantaneous velocity, positive/negative/zero velocity, v(t) = dx/dt
- **Interactive elements:** Drag point along x(t) curve; live tangent line; velocity readout; secant/instantaneous mode; region prediction (positive/negative/zero velocity); velocity sign sort
- **Lesson 1 detailed 10-step flow:**
  1. Hook: "A cart moves along a track. Where is it moving fastest?"
  2. Explore: drag a point on the x(t) curve; tangent appears live
  3. Predict: tap the steepest region (wrong answer feedback: "Look for steepest slope, not highest position")
  4. Average velocity: drag two time markers; see secant slope
  5. Instantaneous velocity: markers shrink; tangent limit appears
  6. Derivative reveal: introduces v(t) = dx/dt
  7. Concept screen: derivative notation
  8. Sort: classify labeled points as positive/zero/negative velocity
  9. Application: predict motion from a new position-time graph
  10. Completion: mastery result, streak update, Lesson 2 unlock

### Lesson 2: Velocity, Acceleration, and Changing Motion
- **Core idea:** Acceleration = derivative of velocity; a(t) = dv/dt
- **Covers:** v(t) graphs, slope as acceleration, speeding up vs. slowing down
- **Interactive:** Adjust velocity graph segments; motion animation responds; identify when object speeds up/slows down

### Lesson 3: Displacement from Area Under Velocity
- **Core idea:** Displacement = integral of velocity; Δx = ∫v(t)dt
- **Covers:** Area under v(t) graph, positive/negative area, net displacement vs. total distance
- **Interactive:** Shade graph regions; drag interval endpoints; compare net displacement and total distance

### Lesson 4: From Acceleration to Velocity and Position
- **Core idea:** Integration builds position from velocity and velocity from acceleration
- **Covers:** v(t) = v₀ + ∫a(t)dt; x(t) = x₀ + ∫v(t)dt; constant acceleration equations; initial conditions
- **Interactive:** Slider for constant acceleration; live a(t)/v(t)/x(t) graph updates; predict final velocity and displacement

### Lesson 5: Motion in Two Dimensions
- **Core idea:** 2D motion = two linked 1D motions
- **Covers:** Position vector r(t); velocity vector v(t) = dr/dt; acceleration vector; components and vector arrows
- **Interactive:** Move object on 2D plane; display x/y position, velocity, and acceleration components; match vector arrows to motion
- **Note:** All subscript notation (v_x, v_y, a_x, a_y) uses the `RichText` renderer for proper `<sub>` tags

### Lesson 6: Projectile Motion
- **Core idea:** Horizontal and vertical motion are independent
- **Covers:** Constant horizontal velocity; vertical acceleration due to gravity; parametric equations; time of flight, peak height, range
- **Interactive:** Launch angle + speed sliders; projectile path updates continuously/live (no launch button); ball animates in loop; stats (peak, range, time of flight) shown live

### Lesson 7: Kinematics Mastery Challenge
- **Core idea:** Combine graphs, equations, derivatives, integrals, and 2D reasoning
- **Covers:** Mixed graph interpretation; translating between x(t), v(t), a(t); AP-style conceptual problems; multi-step reasoning
- **Interactive:** Build missing graph from a given graph; multi-step motion scenario; review weak areas

---

## Content Model (TypeScript, version-controlled — never in Firestore)

```ts
Course {
  id: string
  title: string
  lessons: Lesson[]
}

Lesson {
  id: string
  title: string
  order: number
  estimatedMinutes: number
  steps: LessonStep[]
}

LessonStep {
  id: string
  type: "concept" | "multipleChoice" | "graphDrag" | "sliderSimulation" | "sort" | "numeric"
  prompt: string
  interactionConfig: object  // curve key, options, tolerance, etc.
  correctAnswer: object
  feedback: {
    correct: string
    incorrect: string
    hint?: string
  }
}
```

- Lesson content is canonical in code; Firestore stores **only user progress**
- Stable deterministic IDs required: e.g. `lesson-1-position-velocity`
- Curve registry (`src/lib/curves.ts`): step configs reference curves by string key (e.g. `"parabola_down"`, `"smooth_increasing"`)
- `RichText` component (`src/components/RichText.tsx`): parses `v_x`, `v_y`, `a_y` → `<sub>` tags; applied to all prompts and feedback text

---

## Firestore Data Model

```
users/{userId}
  displayName, email, createdAt, lastActiveAt

users/{userId}/lessonProgress/{lessonId}
  status: "not_started" | "in_progress" | "completed"
  currentStepIndex
  completedStepIds
  attemptsByStep       // Map<stepId, number>
  correctByStep        // Map<stepId, boolean>
  lastUpdatedAt
  completedAt

users/{userId}/courseProgress/kinematics
  unlockedLessonIds
  completedLessonIds   // Source of truth for "Completed" badge; never removed on restart
  currentLessonId
  masteryByLesson      // Map<lessonId, number 0–1>
  updatedAt

users/{userId}/streaks/current
  currentStreak
  longestStreak
  lastActivityDate
  milestoneIds         // Array of earned milestone IDs
```

### Data Flow
1. User signs in via Firebase Auth
2. React loads local course content from TypeScript files
3. App reads Firestore progress for authenticated user
4. Lesson renderer combines local content + user progress
5. On every step completion, React writes progress to Firestore
6. On lesson completion: Firestore updates completedLessonIds, updates streak + BEST mastery (does NOT unlock the next lesson — that requires passing the quiz at ≥70%; see "Mastery + quiz gating")
7. On return: app restores user to saved lesson and step

---

## Key Architectural Decisions

- **Content canonical in code; only progress in Firestore** — keeps lesson definitions version-controlled, fast to render, AI-generatable later
- **Completion badge based on `completedLessonIds`** — never removed when restarting a lesson; replays don't break the "Completed" indicator
- **Restart clears lessonProgress doc** (resets step tracking) but does NOT modify `completedLessonIds` or unlock logic
- **Lesson engine phases:** `play` → `summary` → `review`; completed lessons open to summary screen instead of auto-restarting
- **`answered` Set replaces `locked` boolean** — tracks which steps have been correctly answered; enables back-navigation with prefilled correct answers
- **MCQ requires Submit button** — selecting an option doesn't auto-grade; user must click Submit (re-submit after wrong answer); prevents spam-clicking
- **Back button in lesson** — "← Previous step" button visible at step > 0; revisiting shows the step in its solved, locked state with correct answer prefilled
- **Lazy route loading** — all page components are `React.lazy()`; Firebase and vendor chunks split manually for <2s first load

---

## Implemented Features (current state)

### Authentication
- Email/password sign-up with display name
- `AuthProvider` with `signUp`, `signIn`, `signOut`
- `RequireAuth` route guard
- Auth page toggles between sign in / sign up

### Lesson Engine (`useLessonEngine` hook)
- Step-by-step progression with auto steps (concept, explore, sims) and graded steps (MCQ, numeric, sort, graphDrag)
- `onAnswer` / `onContinue` / `onBack` callbacks
- `prefillCorrect` flag passed to graded step components when reviewing
- Feedback stored per-step in Firestore (`attemptsByStep`, `correctByStep`)
- Resume mid-lesson: returns to exact saved step
- Lesson completion: awards milestones, records daily streak activity
- Phase management: `play` | `summary` | `review`

### Step Components
- **MultipleChoiceStep** — selection + Submit button flow; wrong answer shows hint/feedback + Resubmit; correct locks and shows Continue
- **NumericStep** — numeric input with tolerance; Check Answer button
- **SortStep** — drag-to-reorder with locked review state
- **GraphDragStep / PositionTimeGraph** — explore mode (free drag), secant mode (two markers), predict mode (tap region); tangent line live; supports `prefillCorrect`
- **ProjectileSim** — continuous animation, sliders always enabled, ball loops along trajectory; stats (peak, range, time-of-flight) update live

### Summary / Review Flow
- Clicking a completed lesson opens summary screen (not auto-restart)
- Summary shows: mastery %, first-attempt vs. retried breakdown, per-step outcome chips
- Two options: **Review** (read-only walk-through, all steps prefilled with correct answers) or **Restart** (fresh attempt, lesson card stays "Completed")
- Review mode shows "← Previous step" / "Next" / "Back to summary" (at last step)
- "Reviewing" badge displayed in progress header during review

### Home Screen / Landing Page
- Blueprint/engineering aesthetic; dark "oscilloscope" hero panel
- Animated SVG motion demo: particle traces a position-time curve with a fading trail and live tangent showing velocity = slope; powered by `requestAnimationFrame`
- Space Grotesk + Inter font pairing
- 7-lesson sequence shown as a real ordered progression
- "Why it works" section with monospace styling
- Dark CTA footer
- Responsive; `usePrefersReducedMotion` hook disables animation if needed

### Badge / Milestone System (21 badges, 5 groups)

**Accuracy:**
- 🎖️ **Sharp Shooter** — score 80%+ on any lesson
- 💎 **Flawless** *(Rare, cyan ring)* — 100% mastery on a lesson
- 🥇 **Triple Ace** — 100% on three different lessons
- 📜 **Honor Roll** — 80%+ on every completed lesson
- 👑 **Perfectionist** *(Legendary, gold gradient)* — 100% mastery on all 7 lessons

**Course Progress:**
- 🎯 **First Lesson Complete** — complete lesson 1
- ⛰️ **Halfway There** — complete 4 lessons
- 🏆 **Course Complete** — finish all 7

**Streaks:**
- 🔥 3-day streak
- 🔥 7-day streak
- ⚡ 2-week streak
- 🌟 30-day streak
- 🏅 **Marathoner** *(Legendary)* — 60-day streak

**Quizzes** (best quiz score per lesson; pass threshold = 80%):
- 🎯 **Quiz Ace** — 100% on any quiz
- 🧠 **Sharp Mind** — 80%+ on any quiz
- 🎩 **Hat Trick** — 100% on 3 different quizzes
- 📚 **Quiz Honor Roll** — 80%+ on all 7 quizzes
- 🏆 **Quiz Champion** — finish all 7 quizzes
- 💯 **Quiz Perfectionist** *(Legendary)* — 100% on all 7 quizzes

**Challenges:**
- 💪 **Comeback Kid** — replay a finished lesson and beat your previous mastery score
- 🔱 **Triple Threat** — complete Learn + Practice + Quiz for the same lesson

Rarity tiers: `rare` (cyan frame + "Rare" label), `legendary` (gold gradient + "Legendary" label). Quiz/Triple-Threat badges are computed by `quizMilestonesFor` / `tripleThreatMilestonesFor` (`src/lib/milestones.ts`) and awarded by `awardProgressMilestones` (`progressService.ts`), called from `QuizPage`/`PracticePage` after a score is recorded. Lesson accuracy/streak/course badges are still awarded on lesson completion.

### Profile Page (`/profile`)
- Accessible via circular avatar button (user initial) in AppShell header, visible on all signed-in pages
- Shows: avatar, display name, email
- Stats: lessons completed, average mastery, day streak, badges earned
- Full grouped badge gallery — earned badges in color, locked badges greyed with 🔒 and unlock requirement

### Dashboard
- MiniStat strip: current streak, longest streak
- Milestone badge chips for earned milestones
- Lesson cards with completion status from `completedLessonIds`

### Infrastructure
- `.gitignore` properly ignores `.env`, `.env.local`, `.env.production`; template `.env.example` files remain tracked
- **e2e Firebase isolation (IMPORTANT):** `playwright.config.ts` `webServer.command` explicitly injects emulator env (`VITE_USE_FIREBASE_EMULATORS=true`, `VITE_FIREBASE_PROJECT_ID=demo-motionlab`, etc.) and sets `reuseExistingServer:false`, so e2e NEVER hits production Auth. (Earlier, `.env.local` pointing at prod caused stray `bob_*@example.com` users to be created in real Auth.)
- Unit + integration (Vitest): ~302 tests passing. Playwright e2e: 9 specs passing.
- Smoke test (`npm run smoke`): builds `dist/` and runs Playwright against `vite preview`
- `npm run deploy` / `npm run deploy:rules` for Firebase Hosting + Firestore rules
- `frontend-design` skill symlinked to `~/.claude/skills/frontend-design`

---

## MVP Non-Functional Requirements

- Answer feedback: **< 100ms**
- Interactive graph/visual updates: **60 FPS target**
- First interaction load: **< 2 seconds** (achieved via lazy route splitting + Firebase/vendor chunking)
- Mobile touch: first-class; responsive on phone-sized screens
- Progress persists after refresh, logout, or device switch
- Multiple concurrent users: no performance impact (content is local; only progress writes hit Firestore)

---

## Explicitly Out of Scope

Chatbot tutor, video lessons, teacher/admin dashboard, payments/subscriptions, social features. (Note: AI-generated *practice problems* are now IN scope via the AI Practice Hub.)

---

## Lesson Modes (Learn / Practice / Quiz)

Each dashboard lesson card is an **expandable dropdown** (`src/components/LessonCard.tsx`) with three modes. Locked lessons are not expandable. The card surfaces best Quiz % and best Practice % per lesson.

### Routes
- `/lesson/:id` — Learn (existing lesson engine)
- `/lesson/:id/practice` — Practice game (`src/pages/PracticePage.tsx`)
- `/lesson/:id/quiz` — Quiz (`src/pages/QuizPage.tsx`)

### Mastery + quiz gating (IMPORTANT — see "Learning Science.txt")
Two performance thresholds, centralized in `src/lib/gating.ts`:
- `LESSON_MASTERY_THRESHOLD = 0.8` and `QUIZ_PASS_PERCENT = 70`.
- Helpers: `modesUnlocked(courseProgress, lessonId)` (best mastery ≥ 80%), `quizPassed(scorePct)` (≥ 70%), `lessonMastery(...)`.

Rules:
1. **Practice + Quiz of a topic unlock only after Learn is mastered at ≥ 80%.** Below 80%, both modes stay locked on the `LessonCard` (note "🔒 Master Learn (80%)") and their routes (`PracticePage`/`QuizPage`) show a "Master the lesson first" guard. The lesson completion screen branches: ≥80% → 🎉 + next CTA; <80% → 📊 "Almost there!" with **Review answers** (`onReview`) and **Redo lesson** (`onRestart`), no forward CTA.
2. **The next topic unlocks only when the quiz is passed at ≥ 70%.** `QuizPage.onComplete` always records the score but calls `unlockNextLesson` **only if `quizPassed(scorePct)`**. Below 70%, `QuizRunner`'s score screen hides the next-step CTA, shows a retake notice (`data-testid="quiz-retry-notice"`), and the retry button reads "Retake quiz". `QuizRunner` takes a `passThreshold` prop (QuizPage passes `QUIZ_PASS_PERCENT`; default 0 = no gate).
- `completeLesson` stores the learner's **BEST** mastery (max of prev and new), so a topic stays unlocked (and accuracy badges stick) even after a weaker replay. `unlockNextLesson(uid, lessonId)` still does the actual unlock.
- `useLessonEngine` exposes `completion.practiceUnlocked` (best mastery ≥ 80%) and now caches the per-step summary on finish so "Review answers" works straight from the completion screen; `onReview` clears `completion` so review steps render.

### Completion chaining: Learn → Practice → Quiz → next lesson
- `src/lib/lessonFlow.ts` `nextDestination(lessonId, mode)` computes the next CTA, gracefully **skipping modes a lesson doesn't have** (uses `hasGame`/`hasQuiz`). learn→practice→quiz→next-lesson; missing modes are skipped; last lesson with nothing after → null (Back to course only). NOTE: the forward CTA is gated by the thresholds above (completion screen only shows it at ≥80% mastery; quiz score screen only shows it at ≥70%).
- Every "next" CTA (Learn completion screen, game result screen, quiz result screen) uses `data-testid="next-step"`.
- The 🎉 celebration completion screen and all "Back to course" links are preserved.

### e2e note (gating)
- `e2e/helpers.ts` `passQuiz(page, lessonId)` answers a quiz **correctly** by importing `getQuiz` from `src/content/quizzes` (quiz modules import only types, so this is safe in Playwright). Progression specs (lesson1, lesson-sequence, full-course) use `passQuiz` to clear 70%. `finishQuiz` (arbitrary answers) is kept for fail-path use. quiz1/practice1 specs first `completeLesson1` (90–100% mastery) so the quiz/practice are unlocked.

### Practice games — registry + contract
- Registered in `src/games/registry.ts` keyed by lessonId via `lazy(() => import("./XGame").then(m => ({ default: m.XGame })))`. Helpers `getGame(lessonId)` / `hasGame(lessonId)`.
- **GameProps** (registry.ts): `{ onScore?: (bestOverall: number) => void; next?: { href: string; label: string } }`.
- A game MUST: be self-contained & presentational (no Firebase imports); call `onScore(best, 0–100)` exactly once when the player finishes; render a result screen with a primary `data-testid="next-step"` react-router `Link` when `next` is provided, plus a "Play again" control and a "Back to course" `Link` to `/`; respect `usePrefersReducedMotion` (skip animation, no infinite rAF) ; be mobile-first; use `requestAnimationFrame` for animation (not CSS transitions) for 60fps.
- Persistence handled by `PracticePage` (`recordPracticeScore` keeps best; counts as daily streak activity). Best score appears on the dashboard dropdown.
- Pure scoring lives in `src/games/lib/*Scoring.ts` with Vitest unit tests.
- **Reference implementation:** `src/games/DriveTheCartGame.tsx` + `src/games/lib/driveScoring.ts` (Lesson 1).

### Drive the Cart (Lesson 1) — current design
- Set a per-second velocity profile (sliders, t=0..6, dt=1) to reproduce a target position-time path across 3 rounds (constant → stop-and-hold → over-the-hill).
- **Difficulty design (important pattern):** NO full target line is drawn — only a few **checkpoint rings** (at t=3 and t=6) are shown, and they turn green when the path passes through them. Scoring still grades the WHOLE curve (RMS error → 0–100), so sparse anchors make it harder, not just leniently scored. New games should follow this "few visible anchors, full-curve scoring" idea where applicable.
- Live score; Play animates the cart (skipped under reduced motion); overall best = average of per-round best scores.

### Quizzes — registry + contract
- Registered in `src/content/quizzes/index.ts`: `getQuiz(lessonId)` / `hasQuiz(lessonId)`.
- **Types** (`src/types/content.ts`): `QuizQuestion { id; type: "multipleChoice"|"numeric"; category: "conceptual"|"calculation"; prompt; graph?; plot?; options?; correctOptionId?; value?; tolerance?; unit?; placeholder?; explanation }`; `Quiz { lessonId; questions }`.
- 8–10 ORIGINAL questions per lesson, mixing conceptual + calculation; every question has a non-empty `explanation`. Must be fully answerable from that lesson's Learn content. May reuse `graph` (GraphConfig) / `plot` (PlotConfig) visuals.
- Engine: `src/hooks/useQuiz.ts` + presentational `src/components/quiz/QuizRunner.tsx` (immediate per-question feedback → final score + per-question review with retry). `QuizPage` wires persistence (`recordQuizScore` best; daily streak) + the `next` CTA.
- Pure grading: `src/lib/quiz.ts` (`gradeQuestion`, `quizScorePct`). **Reference:** `src/content/quizzes/quiz-1-position-velocity.ts` + `src/content/quizzes/__tests__/` / `src/content/__tests__/quiz-content.test.ts`.

### Practice games by lesson (names + concept)
- L1 **Drive the Cart** — build a velocity profile to hit position checkpoints (DONE)
- L2 **Acceleration Pilot** — set an acceleration profile to match a target velocity curve (a = dv/dt)
- L3 **Area Painter** — choose/shade the area under v(t) to hit a target displacement (signed area; net vs total)
- L4 **Integral Stack** — integrate a(t) → v(t) → x(t); tune acceleration (+ v₀/x₀) to hit position targets
- L5 **Coordinate Courier** — set v_x, v_y (and/or a_x, a_y) to route to 2D waypoints (independent components)
- L6 **Cannon Range** — pick launch angle + speed to hit targets at varied range/height (projectile independence)
- L7 **Boss Rush** — a fast scored gauntlet of mixed mini-challenges across all prior topics

### Progress fields
- `CourseProgress.quizScores` and `CourseProgress.practiceScores` (Map<lessonId, best 0–100>) in `src/types/progress.ts`; writers `recordQuizScore` / `recordPracticeScore` in `progressService.ts`; surfaced via `useProgress` on the dashboard dropdown.

### Reusable building blocks for games/quizzes
- Graphs: `PositionTimeGraph` (modes static/explore/secant/predict), `PlotGraph` + `StaticPlot` (generic f(t) plots with regions/area shading).
- Math: `src/lib/functions.ts` (`integrate`, `totalDistance`, plot presets), `src/lib/curves.ts` (`niceTicks`, curve registry, `averageVelocity`).
- Existing sims (patterns to adapt): `MotionSim`/`AccelerationSim` (L2), `AreaSim` (L3), `KinematicsSim` (L4), `Vector2DSim` (L5), `ProjectileSim` (L6).
- `Slider` (supports `disabled`), `Feedback`, `RichText` (`v_x`→subscript), `usePrefersReducedMotion`.

### Adding a game / quiz — file conventions
- Game: `src/games/<Name>Game.tsx` (named export `<Name>Game`); scoring `src/games/lib/<name>Scoring.ts`; tests under `src/games/lib/__tests__/` (+ optional component test). Then register in `src/games/registry.ts`.
- Quiz: `src/content/quizzes/quiz-<n>-<slug>.ts` (export `quiz<n>`); test `src/content/quizzes/__tests__/quiz-<n>.test.ts`. Then register in `src/content/quizzes/index.ts`.
- If you add/alter Learn steps, ALSO update the matching `completeLessonN` walkthrough in `e2e/helpers.ts` (step counts/answers) or the e2e suite breaks.
- `motion-graphics` skill (`~/.agents/skills/motion-graphics`) is **not used** — it renders MP4 videos, not interactive games.

---

## Top Navigation Tabs (AppShell)

`AppShell` renders a sticky tab row (signed-in only): **Course** (`/`), **Practice** (`/practice`), **Games** (`/games`), **Leaderboard** (`/leaderboard`), using `NavLink` (`TabLink` helper) with active styling. The header wordmark, "Sign out", profile avatar, and streak chip were intentionally enlarged.

- **Container width (responsive)**: the shell is `mx-auto ... w-full max-w-6xl` with responsive horizontal padding (`px-4 sm:px-6 lg:px-8` on header/main, similar on nav) — widened from the old skinny `max-w-2xl` so the app uses more of the screen on desktop while staying full-width/single-column on phones. Individual content pages (Dashboard/Lesson/Practice/Quiz/Profile/Games/Leaderboard) constrain their OWN inner reading width / use multi-column grids so prose stays readable and pages look intentional at the wider shell (this was a `/frontend-design` pass). Game pages self-size their canvas (see basketball sizing) and just center in the wider shell.

- The lesson-card mode links carry `data-testid="lesson-mode-{learn|practice|quiz}"` so the top-nav "Practice" tab doesn't make those e2e selectors ambiguous.

## AI Practice Hub (`/practice`) — adaptive AI problems

A separate surface from the per-lesson Practice **games** (`/lesson/:id/practice`). Student picks a **topic** (any of the 7 lessons) + **difficulty** (easy/medium/hard); an AI generates one problem at a time with immediate feedback (graded client-side like a lesson step). Adaptive nudges: a trailing run of correct answers suggests leveling up (unless on hard); a run of wrong answers suggests leveling down, or — when already on easy — going back to the lesson to review.

- **Shared contract (`src/ai/`):**
  - `practiceTypes.ts` — `Difficulty`, `DIFFICULTIES`, `AIPracticeQuestion`, `PracticeTopic`, `GenerateParams` (dependency-free).
  - `topics.ts` — `practiceTopics()` derives topics from `course.lessons` (id, title, blurb=coreIdea).
  - `adaptive.ts` — pure `adaptiveSuggestion(results, difficulty)` (+ `harderDifficulty`/`easierDifficulty`, `ADAPTIVE_STREAK=3`). Unit-tested in `src/ai/__tests__/adaptive.test.ts`.
  - `practiceService.ts` — `generatePracticeQuestion(params): Promise<AIPracticeQuestion>` calls the **`generatePracticeQuestion` Firebase Cloud Function** (a server-side OpenAI proxy, `gpt-4o-mini`) via `httpsCallable` (NOT Firebase AI Logic/Gemini anymore — the key lives server-side). Falls back to the static practiceBank on error.
  - The function's prompt (`functions/index.js` → `buildPrompt`/`DIFFICULTY_GUIDANCE`/system message) is tuned to emit **AP-exam-style** kinematics items: concrete scenarios, AP reasoning patterns, and (for MC) 4 plausible options whose 3 distractors are based on specific common student mistakes (dropped ½, distance vs. displacement, v vs. v², sign errors, "a=0 at the peak"). This is prompt-only (no schema change). Output still passes the `mathjs` answer-verification gate, so AP-style prompting cannot ship a wrong answer. **Prompt edits only take effect after `npm run deploy:functions`.**
- **GLOBAL AI TOGGLE (off by default) — `src/lib/aiSettings.ts`:** `isAiEnabled()` (non-React getter, localStorage key `motionlab.aiEnabled`, default FALSE), `setAiEnabled(b)`, `useAiEnabled(): [bool, setter]` (live via `useSyncExternalStore` + cross-tab `storage` event). UI: reusable `src/components/AiToggle.tsx` (`role="switch"`) in the header (`AppShell`, `data-testid="ai-toggle"`) and Profile "Settings" (`data-testid="ai-toggle-profile"`). **The MVP runs entirely on the static bank with AI OFF.**
  - **Gating — `src/ai/practiceQuestion.ts#getPracticeQuestion(params): Promise<{source:"ai"|"bank", question}>`:** AI off → bank directly with NO network call; AI on → try the Cloud Function then fall back to bank. Used by `useLabSurvival` and the pool full-game; `useAdaptivePractice` gates inline. This is the preferred entry point for any new feature that wants questions.
  - **AI math correctness (`functions/index.js` + `mathjs@^15.2.0`):** after the model answers, the function recomputes the arithmetic from the model's own explanation and confirms the declared numeric/MC answer agrees; on contradiction it regenerates up to 3× and otherwise throws `failed-precondition` so the client falls back to the bank — so **a wrong math answer never ships**. Conceptual/non-computable questions pass through. ⚠️ Needs `npm install` in `functions/` + redeploy of the function to take effect live.
- **UI:** `src/pages/PracticeHubPage.tsx` (+ `src/hooks/useAdaptivePractice.ts`). Picker → runner (loading / question / feedback / adaptive banner / error+retry).
- **Testability:** the Cloud Function is live-only (needs the deployed function + `OPENAI_API_KEY` secret). Tests **mock** `../ai/practiceService` / run with AI OFF (bank). Service handles failures gracefully (friendly error + retry).

## Games Hub (`/games`) + arcade games

`src/pages/GamesPage.tsx` — a picker (`data-testid="games-page"`) linking to three full arcade games (cards `game-card-pool`, `game-card-basketball`, `game-card-cannon`). These are SEPARATE from the per-lesson practice games on lesson cards.

### Shared arcade foundation (`src/games/arcade/`)
- `audio/audioEngine.ts` — **procedural Web Audio** (no audio files): `resumeAudio()` (call from a user gesture), `setMuted`/`isMuted`, `playSfx(name)`, `startMusic(track)`/`stopMusic()`, and two distinct loops `POOL_TRACK` (cool lounge groove) / `BASKETBALL_TRACK` (energetic). `audio/useArcadeAudio.ts` React wrapper + `MuteButton.tsx`.
- `useGameLoop.ts` — rAF loop hook `useGameLoop(cb(dt), running)` with clamped dt.
- `useHighScore.ts` + `services/gameScoreService.ts` — best score per gameId in Firestore (`users/{uid}/gameScores/arcade`) with a localStorage fallback (works logged-out/offline). NOTE: it does NOT auto-post to the public leaderboard anymore — that's an explicit opt-in from the game-over screen (see Leaderboard below).
- `types.ts` — `ArcadeGameProps { highScore; onGameOver({score}); leaderboard? }`. The optional `leaderboard: ArcadeLeaderboard { playerName; playerId; publish(score); fetchTop(topN?) }` lets a game OFFER to post the score + show the board without importing Firebase (the page injects the Firestore-backed callbacks). Pages (`PoolGamePage`/`BasketballGamePage`/`CannonGamePage`) wrap the game in `AppShell` and persist the score via `useHighScore`. Games themselves NEVER import Firebase.
- All arcade games are React + **HTML Canvas with custom deterministic physics** (NOT Phaser), built per the `game-designer` + `physics-intuition` skills (juice, readable-at-thumbnail, real arcs/weight), and respect `usePrefersReducedMotion`.

### Physics Pool (`/games/pool`) — TWO MODES behind an in-game mode select
- `pool/PoolGame.tsx` is now a **thin shell** (root `data-testid="pool-game"`): a "Pool Lab" mode-select screen with two cards — **One-Shot Challenge** (`pool-mode-oneshot`) and **Full Game (vs Computer)** (`pool-mode-full`). Picking Full Game shows a difficulty chooser (`pool-diff-easy|medium|hard`). Every sub-game has a "Menu" back button (`pool-back-modes`/`pool-back-menu`). The shell keeps the page contract (`highScore`/`onGameOver`) unchanged.
- **One-Shot Challenge** (`pool/OneShotGame.tsx`, root `data-testid="pool-oneshot"`, `pool-shoot`): the ORIGINAL calculation puzzle, extracted verbatim (behavior preserved). Authentic cue→object-ball→pocket on a coordinate-plane table; the shot is CALCULATED and entered numerically (angle° + speed); HUD shows cue/object/target-pocket coords, ball radius, friction `a`; help panel teaches the **ghost-ball** aim and **v=√(2·a·d)**; after each shot it reveals the ideal angle/speed + the player's angle error.
- **Full Game vs AI** (`pool/FullPoolGame.tsx`, root `data-testid="pool-full"`): GamePigeon-style 8-ball — standard rack + cue + 6 pockets, alternating player/AI turns (`pool-turn`; pot continues the turn, miss/scratch passes it with the cue re-spotted), sink your group then the 8 to win. Win/lose screen offers replay (`pool-full-replay`) or menu. `onGameOver` reports `balls-potted × 100 + 500 win bonus`.
  - **CONTROL SCHEME (decoupled — important):** the canvas does ONE thing, AIM. Pointer MOVE over the table live-aims (cue→pointer) ONLY while UNLOCKED; a CLICK on the table TOGGLES an explicit **aim lock** (`aimLocked` state) — once locked, pointer moves are ignored so sliding down to the power bar can't re-aim (click again to unlock). It NEVER sets power and NEVER fires. **Power** is owned solely by a **draggable gradient power bar** (`data-testid="pool-power-bar"`, `powerFromPointer` sets it from pointer-x; the `pool-power` range `<input>` is kept as an accessible mirror). **Shooting** fires ONLY from the Shoot button (`pool-full-shoot` → `shootFromControls`), guarded by `MIN_SHOOT_POWER`. (There is NO table click-to-shoot and NO distance→power.) On-canvas banner `data-testid="pool-aim-state"` reads "Move to aim · click to lock" → "Aim locked · drag power · press Shoot" (amber when locked; the locked aim guide renders as a solid amber beam). Lock resets to unlocked on every `beginShot`, `replay`, and AI concede.
  - **LEARNING (per-turn questions → perks):** before EVERY player aim phase (the break, "go again", and after the AI passes — never on AI turns or game over) a question modal opens (`pool/QuestionModal.tsx`, `data-testid="pool-question-modal"`): pick difficulty (`pool-q-difficulty-easy|medium|hard`) → question (random topic across all 7 lessons) → grade. Questions come from `getPracticeQuestion(...)` so **AI on = AI question (bank fallback), AI off (default) = static bank, no network**. A **Skip** (`pool-q-skip`) never hard-blocks play. Correct answer → +50/100/150 score (flows to `onGameOver`) AND choose ONE perk (`pool/poolPerks.ts`): **Aim Assist** (`bestMakeableShot` scans your group × pockets with `computeIdealShot` incl. cut-angle + line-of-sight checks → draws a cyan suggested aim/ghost-ball/pocket overlay), **Ball in Hand** (drag the cue to a `legalCuePlacement` spot, gated by a "Done placing" `pool-place-done` button), or **Scratch Shield** (`consumeUndoScratch` one-shot token: keep your turn on a scratch). Active perk shows a `pool-perk-badge`. All per-turn state resets across turns/AI/replay/over.
  - **Power band:** `MIN_SPEED=14`, `MAX_SPEED=330` (a full-power break reaches the rack apex at ~327 u/s and genuinely scatters the whole rack to the far rails); the AI is capped at `AI_MAX_SPEED=150` for fairness. The sim's sub-stepping keeps fast breaks stable (no tunneling even at 6000 u/s).
  - **SPIN / English (question-gated perk):** a 4th perk choice "Add Spin 🌀" (after a correct answer). When chosen, a draggable mini cue-ball selector (`pool-spin-selector`, with `pool-spin-dot`/`pool-spin-label`/`pool-spin-reset`) sets the contact point: top=follow, bottom=draw, sides=english, center=none. Physics: the shot carries optional `spin:{x,y}∈[-1,1]`; `applyShot` tags the cue with a transient `Ball.pendingSpin` ONLY when non-zero, consumed on the cue's FIRST object-ball contact — adds `fwd·spin.y·speed·0.5` (follow/draw) + `right·spin.x·speed·0.32` (side), scaled by impact speed. When spin is none/`{0,0}` the cue is never tagged, so behavior is byte-for-byte identical to before (existing physics tests + One-Shot unchanged). `ShotInput.spin` / `Ball.pendingSpin` / `beginShot` 3rd arg are all OPTIONAL.
  - **Aim-line REFLECTION preview:** pure `predictAim(balls, table, cueId, angleDeg): AimPrediction` (in `poolPhysics.ts`) ray-casts from the cue center and returns the first hit — a BALL (ghost-ball contact point, struck `ballId`, `objectDir` along the line of centers, and the equal-mass `cueDir` ≈ 90° tangent rule) or a CUSHION (rail point + reflected `cueDir`). The component computes it in `buildScene` and passes it via `SceneState.aimPredict`; `drawAimLine` renders a dotted line to a faint **ghost ball**, a colored object-travel ray, and a white cue-carom/bank ray. (Great hook for teaching the 90° rule / ghost-ball method.)
  - **Rolling ball animation:** balls carry an optional cosmetic `roll`/`rollDir`; the game loop accumulates `roll += speed·dt/radius` (slows out with friction) and `drawBall` orbits surface freckles + slides the stripe band + bobs the number (damped ×0.3 so fast spins don't strobe and the number stays legible). One-Shot leaves `roll` unset → no spin there.
  - AI: `pool/poolAi.ts` (pure, seedable RNG) scores every makeable (ball, pocket) by cut angle + distance, then difficulty scales BOTH selection (Hard=best, Medium=top-3, Easy=top-8 random) AND aim/speed error (aim noise Easy ±11° / Medium ±4.5° / Hard ±1°). Targets own group, switches to the 8 once cleared, plays a safety when nothing's makeable.
  - Pure rules: `pool/poolGameLogic.ts` (rack, group classification, `resolveTurn`, win detection, cue re-spot) — split out of React for testing.
- Shared physics `pool/poolPhysics.ts` is REUSED by both modes (deterministic sim with sub-stepping, equal-mass elastic collisions, friction, cushions, pockets; `computeIdealShot` ghost-ball; `gradeShot`; `simulateToSettle`; `predictAim`; optional `stripe`/`roll` ball flags). `poolRender.ts` draws the striped-ball band, the aim-reflection guide (`SceneState.aimPredict`), and rolling balls; `audioEngine.ts` has a `rackBreak` SFX for the opening break.
- Tests: `pool/__tests__/PoolGame.test.tsx` (mode-select + full-game question-modal + spin-perk flow) + `poolGameLogic.test.ts` + `poolAi.test.ts` + `poolPhysics.test.ts` (incl. `predictAim` + spin: none=unchanged / follow-forward / draw-back / side / speed-scaled) + `poolPerks.test.ts` (best-shot, blocked-lane, legal placement, scratch-shield, spin perk) — 70 pool tests total.

### Buzzer Beater basketball (`/games/basketball`)
- **FRONT-FACING redesign** (GamePigeon-style + Madden-kick aiming). You look straight at a hoop mounted on a brick arena wall: backboard + rim centered up top, net hanging below, the shot **clock floats ABOVE the hoop**, and the ball spawns on a **shooting line near the bottom at a RANDOM x** each shot.
- Pick a clock (30/60/120s). Per shot, TWO timed inputs (Space/tap/click; button reads "LOCK AIM" → "SHOOT!"):
  1. **Aim** — a glowing dotted **parabola (the aim arc)** sweeps side to side at a FIXED `trueSpeed` (the speed that scores from the dead-on direction), so the launch DIRECTION is the skill: you time the lock when the arc drops through the hoop. Its angle range ADAPTS to the ball's spawn x (`aimRange`): a centered ball sweeps around straight-up; a far-left ball sweeps through up-and-right directions (mirror for far-right).
  2. **Power** — a vertical meter whose **green band is the true-aim MAKE window**, computed by sampling speeds with `solveSpeedWindow(centerRad)` and keeping the contiguous range that actually goes in. The band is fixed at shot start (does NOT depend on the locked direction), so "stop it in the green" genuinely corresponds to a make WHEN the aim is true. **The aim arc does NOT oscillate during the power phase** — it freezes at lock (locked dir + `trueSpeed`) and only the meter moves; the arc is green/red per `aimGood = simulateShot(lockedDir, trueSpeed).made`.
- **Real projectile physics + REALISTIC BACKBOARD + rim collision + RATTLE** (`simulateShot`, deterministic, no rng): the ball integrates under gravity. The **backboard is now a genuine front-face collision plane** (`faceX = hoopX + travelSign·rimRx·0.32`, active only while the ball is within the board's vertical/horizontal extent): on contact the horizontal velocity **reflects with energy loss** (`BOARD_REST = 0.58`) and vertical is damped but keeps direction (`BOARD_VKEEP = 0.72`), then it keeps falling — so a flush, well-paced ball banks DOWN into the rim (`bankIn`) but an off-angle/over-powered ball caroms back toward the shooter, AWAY from the hoop (no more "everything funnels to the hoop"). A `banked` flag ensures one clean carom. Clean swishes always crest in FRONT of this plane, so they never falsely bank. Each descending rim-plane crossing is classified — center ⇒ **swish**, within the ring ⇒ **rim-in**, far side ⇒ **bank in**. A graze in the clip zone (rimRx…rimRx+ballR) **bounces** (toward center near the hole, outward at the edge, damped, up to 2 contacts) and may drop in later or clank out. No new `ShotResult` variant — caroms-away reuse `wide`/`long`/`rimOut`. Outcome drives banners, particles, net swish (`netStretch`), and audio.
- Every 15s (game clock PAUSES) a question modal appears: pick easy/medium/hard → a question from the practice bank → correct adds time (+2/+5/+10), wrong −5s. Clock to 0 ends it; tracks high score.
- **Module split (frozen contracts so physics + renderer stay in sync):**
  - `basketball/basketballLayout.ts` — geometry source of truth: `ASPECT_W/H`, `Layout`, `computeLayout`, `randomSpawnX`. Hoop/rim/board/spawn-line positions, plus `gravity`/`speedMin`/`speedMax` (all scale with canvas height for resolution independence).
  - `basketball/basketballScene.ts` — the render contract: `BballScene` (incl. `aimPreview`, `aimLocked`/`aimGood`, `power`/`powerGreen`/`powerHalf`, `netSway`/`netStretch`, `ballScale`), `Phase`, `Screen`, `Particle`/`FloatText`/`CrowdSpeck`/`Banner`.
  - `basketball/basketballPhysics.ts` — pure math (`aimRange`, `aimAngleAt`, `launchVelocity`, `idealSpeed`, `powerToSpeed`/`speedToPower`, `solveSpeedWindow` [the green make-window], `greenBand`/`aimGood` [legacy helpers, still exported], `simulateShot` → `ShotResult`/`ShotSim` [backboard front-face reflection w/ energy loss + up-to-2 rim rattle bounces; consts `BOARD_REST`/`BOARD_VKEEP`], `previewArc`, `applyTimeDelta`, pacing helpers, consts `TIME_BONUS`/`WRONG_PENALTY`/`TIME_LIMITS`/`QUESTION_INTERVAL`) — **angles are radians from +x in a y-UP sense (π/2 = straight up); screen y grows downward.** Unit-tested (43 tests: adaptive aim, true-aim make-window scoring across spawns, rim-rattle spectrum swish→rim-in→bounce-out, **backboard carom-AWAY on off-target overthrows + flush bank-in**, short/long misses).
  - `basketball/basketballRender.ts` — pure canvas painter `drawScene(ctx, layout, scene)` (front-facing arena/crowd/spotlight, backboard, two-piece rim with net hanging inside, net swish, ball + trail, the sweeping aim arc, dynamic power meter, clock-above-hoop HUD, particles/banner/flash). Built with the `/game-designer` + `/physics-intuition` skills.
  - Component `basketball/BasketballGame.tsx` (+ `QuestionModal.tsx`) owns simulation/state; mirrors `phase` into `uiPhase` state for the button label. Root `data-testid="basketball-game"`, controls `bball-start`/`bball-action`.
  - **Sizing**: the court is no longer pinned at 420px — its wrapper uses `style={{maxWidth:"min(36rem, calc((100svh - 15rem) * 0.8))"}}` so it grows to fill the screen on desktop yet stays capped by viewport HEIGHT (it's a tall 4:5 portrait court) and goes full-width on phones. The end `Overlay` is `overflow-y-auto` so the richer game-over content never clips.
  - `basketball/EndLeaderboard.tsx` — game-over leaderboard **opt-in** (pure UI; uses the injected `leaderboard` callbacks, no Firebase). Flow: ask ("Add your score? Posting as {name}") → on "Add my score" (`bball-add-leaderboard`) it `publish`es then `fetchTop(8)` and shows a mini board (`bball-end-leaderboard`) with the player's row highlighted alongside other users; "No thanks" dismisses; errors offer retry. Unit-tested `basketball/__tests__/EndLeaderboard.test.tsx` (4 tests).

### Cannon Duel (`/games/cannon`)
- 1v1 artillery duel: player's cannon on the left ridge, AI's on the right, a procedurally generated landscape between them. Drag to aim + set power (short dotted preview), answer physics questions to earn ammo/shields. React + Canvas, deterministic. Root `data-testid="cannon-game"`; component `cannon/CannonGame.tsx` (+ `QuestionModal.tsx`), modules `cannonLayout.ts` / `cannonPhysics.ts` / `cannonRender.ts` / `cannonScene.ts`. `CannonGamePage` wraps it; `gameId = "cannon"`.
- **Terrain is GUARANTEED solvable from BOTH cannons** (fixes "borderline impossible maps"). `cannonLayout.ts` still exposes the raw `generateTerrain(lay, rng, forced?)` (used by tests), but gameplay now calls `generateSolvableTerrain(lay, rng, forced?)` (same signature) from `cannonPhysics.ts` at all three call sites in `CannonGame.tsx` (initial ref, `beginPlayerRound`, `startGame`).
  - Solvability check `isTerrainSolvable` / `playerCanHit` / `aiCanHit` REUSES the real `simulateShot`/`idealSpeed` (no divergent model): for each side it samples the cannon's actual allowed angle window (`PLAYER_DIR_MIN..MAX`, `AI_DIR_MIN..MAX` — now exported from `cannonPhysics` as the single source of truth) × powers `[speedMin..speedMax]` (`DEFAULT_SOLVE_GRID = {angles:32, powers:16}` → 561 samples/side) PLUS an analytic `idealSpeed` lob pass per angle; solvable iff some launch returns `"hitTarget"` (not blocked by ground first).
  - `generateSolvableTerrain` generates up to **12 natural fields** and returns the first that passes both sides (preserves variety/looks). If none pass it **carves the central peaks** between the cannons down through progressive caps (`carveCentral`, clip+smooth, re-check), grounded in the physics bound (max apex ≈ `0.79·h` for `speedMax=h·1.78`, `gravity=h·2.0`). Worst-case fallback `flattenSolvable` (gentle low profile `0.82`) so it can NEVER loop forever or return an impossible field. Deterministic given an `rng` seed.
- Tests `cannon/__tests__/cannonPhysics.test.ts` (25 tests): robustness suite asserts solvability from both sides over **240 seeds × 4 canvas sizes (960 fields)** + **all 5 TerrainStyles × 60 seeds (300 fields)**, plus determinism/in-bounds checks; existing AI-accuracy/preview/economy/scoring tests unchanged.

### Cross-user Leaderboard (`/leaderboard`)
- Nav tab "Leaderboard" (`AppShell`, after Games) → `src/pages/LeaderboardPage.tsx` (default export, behind `RequireAuth`). A game picker (`lb-game-basketball`/`lb-game-pool`, default basketball) + a top-20 list (`data-testid="leaderboard-page"`/`leaderboard-list`, rows `lb-row-{uid}`), medal-tinted ranks, current user's row highlighted with a "You" badge, loading + empty states.
- **Separate PUBLIC store** (the per-user `users/{uid}/gameScores/arcade` doc is private-by-rules, so it can't power a cross-user board). `src/services/leaderboardService.ts`: model `leaderboards/{gameId}/entries/{uid}` (doc id = uid) shaped `{ uid, name, score, updatedAt }`. API: `recordLeaderboardScore(uid, name, gameId, score, fdb?)` (upsert, only RAISES the score, name capped 24 chars / "Anonymous"), `getLeaderboard(gameId, topN=20, fdb?)` (subcollection `orderBy(score desc) limit` — single-field auto index, NO composite index needed), `LeaderboardEntry`, `LEADERBOARD_GAMES`.
- **Posting is OPT-IN** (not silent): after a basketball game ends, `EndLeaderboard` asks the user; only on confirm does it call `recordLeaderboardScore(uid, name, "basketball", score)` and then show the board. `BasketballGamePage` builds the `leaderboard` prop from `useAuth` + `leaderboardService` (`name = displayName || email-prefix || "Anonymous"`). (Pool doesn't wire the opt-in yet — the mechanism is generic, so add the same `leaderboard` prop to `PoolGamePage` to enable it there.)
- `firestore.rules`: signed-in users may READ any leaderboard entry; a user may create/update ONLY their own entry (`uid == auth.uid`, validates score:number≥0, name:string), never delete. **⚠️ MUST be deployed for the live app to work** — the rules are written but NOT yet deployed (the live leaderboard reads/writes are denied until then). Deploy requires re-auth: `firebase login --reauth` then `npm run deploy:rules` (the Firebase CLI token is currently expired). Works against the local emulator already (it reads the rules file directly). Unit test `src/pages/__tests__/LeaderboardPage.test.tsx`; emulator integration test `src/services/__tests__/leaderboardService.integration.test.ts`.

### Practice question bank (`src/content/practiceBank/`)
- `types.ts` `BankQuestion extends QuizQuestion { topicId; difficulty: "easy"|"medium"|"hard" }`; `index.ts` `bankQuestions()`, `questionsByDifficulty(d)`, `getRandomQuestion(d, exclude?, rng?)`. ~120 original, self-contained questions across all 7 topics × 3 difficulties (graded by `lib/quiz.gradeQuestion`). Powers the basketball/cannon/pool questions AND is the AI-practice fallback (used directly when the AI toggle is OFF). **Easy to extend** — drop more `BankQuestion`s into `BANK`. e2e: `e2e/games.spec.ts`.
  - `BANK` is organized in three waves, each grouped by lesson: the original set, a "second wave" of additions, and an **"AP-STYLE WAVE"** (added last). The AP wave is 21 exam-flavored items (3 per lesson = 1 easy/1 medium/1 hard) written fresh — NOT scraped from College Board (AP Classroom is copyrighted + auth-gated; do not scrape it). They deliberately drill AP reasoning: ratio reasoning (stopping distance ∝ v² → 2v gives 4d; range ∝ v₀²), independence of x/y motion, motion symmetry, sign of a vs. v (speeding up/slowing down), and calculus on a given x(t)/v(t)/a(t).
  - id scheme is `bank-<lessonNumber>-<difficulty>-<n>`; when adding more, continue each lesson/difficulty's counter so ids stay unique (the `bank.test.ts` integrity suite enforces unique ids, 4-option a–d MCQs, finite numeric value + positive tolerance + non-empty unit, valid topicId/difficulty, ≥15 per difficulty). Every numeric answer must be hand-verified (the static bank is NOT run through math.js — only AI output is).

## File Structure Highlights

```
src/
  auth/           AuthProvider, RequireAuth
  ai/             AI Practice: practiceTypes, topics, adaptive, practiceService (OpenAI Cloud Function proxy), practiceQuestion (getPracticeQuestion gating by aiSettings)
  lib/            firebase, aiSettings (global AI on/off toggle, off by default), quiz grader, ...
  components/     ...incl. AiToggle (header + Profile switch)
    steps/        MultipleChoiceStep, NumericStep, SortStep, GraphDragStep
    steps/sims/   ProjectileSim, Vector2DSim (L5), KinematicsSim (L4), AreaSim (L3), MotionSim (L2)
    quiz/         QuizRunner.tsx (presentational quiz flow + score/review)
    graph/        PositionTimeGraph, PlotGraph, StaticPlot
    feedback/     Feedback.tsx
    LessonCard.tsx  Expandable Learn/Practice/Quiz dropdown
    RichText.tsx  Subscript renderer (_x, _y → <sub>)
    Slider.tsx    Supports `disabled` prop
  content/
    lessons/      lesson-1.ts through lesson-7.ts
    quizzes/      quiz-1.. + index.ts (getQuiz/hasQuiz)
    practiceBank/ ~120 tagged standalone questions incl. AP-style wave (getRandomQuestion) — powers games + AI-off fallback
    course.ts     Course registry (getLesson, getNextLessonId)
  games/          Per-lesson practice games: DriveTheCartGame.tsx, registry.ts (getGame/hasGame), lib/*Scoring.ts
    arcade/       Standalone arcade games (Canvas + custom physics): pool/ (mode shell + OneShotGame/FullPoolGame/poolAi/poolGameLogic/poolPerks/QuestionModal), basketball/, cannon/, audio/ (procedural),
                  useGameLoop, useHighScore, MuteButton, types (ArcadeGameProps)
  hooks/
    useLessonEngine.ts   Core lesson state machine
    useQuiz.ts           Quiz state machine
    useProgress.ts       Firestore progress reads + statusOf
    usePrefersReducedMotion.ts
  lib/
    curves.ts     Curve registry + derivative utilities (niceTicks, averageVelocity)
    functions.ts  integrate, totalDistance, plot presets
    quiz.ts       gradeQuestion, quizScorePct
    lessonFlow.ts nextDestination(lessonId, mode) — Learn→Practice→Quiz→next chaining
    lessonEngine.ts  computeResumeIndex, buildLessonSummary, masteryMilestonesFor, accuracyMilestonesFor
    milestones.ts    Milestone catalog (21 badges, 5 groups incl. Quizzes; rarity tiers)
    streak.ts     computeStreak + milestone wiring
  pages/
    LandingPage.tsx    Home screen with animated motion demo
    DashboardPage.tsx  Lesson cards + stats strip
    LessonPage.tsx     Lesson renderer + SummaryScreen + CompletionScreen
    PracticePage.tsx   /lesson/:id/practice — loads game from registry, persists score
    QuizPage.tsx       /lesson/:id/quiz — renders QuizRunner, persists score (unlocks next lesson here)
    PracticeHubPage.tsx /practice — AI adaptive practice
    GamesPage.tsx      /games — arcade picker; PoolGamePage /games/pool; BasketballGamePage /games/basketball; CannonGamePage /games/cannon
    LeaderboardPage.tsx /leaderboard — cross-user high-score board (game picker)
    ProfilePage.tsx    /profile route
    AuthPage.tsx
  services/
    progressService.ts  Firestore read/write; completeLesson, unlockNextLesson, resetLessonProgress,
                        awardMilestones, awardProgressMilestones, recordQuizScore, recordPracticeScore
    gameScoreService.ts arcade high scores (users/{uid}/gameScores/arcade)
    leaderboardService.ts public cross-user board (leaderboards/{gameId}/entries/{uid})
  lib/firebase.ts  Firebase init (auth+firestore) with emulator auto-connect; exports `app` (used by AI Logic)
e2e/            Playwright specs (lesson1, lesson-sequence, full-course, streak-milestones, quiz1, practice1, games)
e2e-smoke/      Smoke spec for production bundle

Lesson IDs: lesson-1-position-velocity, lesson-2-velocity-acceleration,
lesson-3-displacement-area, lesson-4-acceleration-to-position,
lesson-5-two-dimensions, lesson-6-projectile-motion, lesson-7-mastery-challenge
```

---

## npm Scripts

```bash
npm run dev          # Vite dev server + Firebase emulators (concurrent)
npm run test         # Vitest unit tests (~302 passing)
npm run test:integration  # Emulator-backed integration tests
npm run e2e          # Playwright e2e tests
npm run smoke        # Build dist/ then Playwright against vite preview
npm run build        # tsc --noEmit + vite build
npm run deploy       # Firebase Hosting deploy
npm run deploy:rules # Firestore security rules deploy
```

---

## Key Decisions & Constraints to Preserve

1. **Lesson content stays in TypeScript files** — never move to Firestore
2. **`completedLessonIds` is the permanent completion source of truth** — restart does not remove from this list
3. **AI is confined to the Practice Hub** — lesson/quiz/game content stays hand-authored; only `/practice` calls a model (Firebase AI Logic / Gemini). Set up App Check before relying on it in production.
4. **MCQ requires Submit** — do not revert to instant-grade-on-selection
5. **Back button restores solved state** — prefillCorrect must work for all graded step types
6. **60 FPS on graphs/sims** — use requestAnimationFrame, not CSS transitions
7. **All content must have stable IDs** — never change existing step/lesson IDs (breaks progress data)
8. **Firestore security rules** — all progress writes scoped to authenticated `userId`
9. **Test-driven** — every new feature needs unit tests + e2e coverage before marking done
